# Local-only handoff / working notes — never publish.
# Pattern, not a list, so new handoff files are covered by default.
*HANDOFF.md

# Local agent/editor config (settings.local.json is machine-specific by design)
.claude/

# macOS
.DS_Store
